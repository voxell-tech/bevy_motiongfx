[package]
name = "motiongfx_interp"
description = "Easing curves and value interpolation."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
readme = "./README.md"

[dependencies]
libm = { workspace = true }

[lints]
workspace = true

[features]
default = ["std"]
std = []
